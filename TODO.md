# TODO (submission prep)

- [x] Inspect key files: `app.py`, `utils/history_logger.py`, `utils/unified_predictor.py`, `training/evaluate_models.py`, `README.md`, `requirements.txt`.
- [x] Cleanup debug logs/scripts that are not required for submission.
- [ ] Clean `requirements.txt` (remove unused deps only if safe).
- [ ] Update `README.md` with required sections (features, architecture, install, dataset, model training, running, evaluation results, project structure, future improvements).
- [ ] Verify prediction history + analytics.
- [ ] Verify evaluation artifacts wiring (`results/compare_models.*` + confusion matrices).
- [ ] Run `python -m compileall -q .`.
- [ ] Run `pytest -q` and capture results.
- [ ] Run Streamlit smoke (`streamlit run app.py` imports/UI render).
- [ ] Final concise report: files modified/removed, test results, remaining issues, ready for submission.

